# ============================================
# BookRank - 纽约时报畅销书排行榜
# 生产环境依赖（Render 免费版优化版）
# 不含开发工具，减少内存占用和安装时间
# ============================================

# 核心框架
Flask==3.1.3
Werkzeug==3.1.6
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-CORS==6.0.0
Flask-Babel==4.0.0

# ORM 和数据库
SQLAlchemy==2.0.51
alembic==1.14.0
psycopg2-binary==2.9.10

# 配置和环境
python-dotenv==1.2.2

# HTTP 客户端
requests==2.33.0

# 系统监控
psutil>=5.9.0

# WSGI 服务器
gunicorn==23.0.0

# HTML 解析
beautifulsoup4==4.13.0

# AI 翻译（智谱AI主用；硅基流动 Hunyuan 走 openai 兼容端点）
zhipuai==2.1.5.20250825
# ⚠️ 不再安装 deep-translator：PyPI 账号被钓鱼接管后发布含安装期恶意代码的
# 版本（PYSEC-2022-252），1.8.5 及之后全部受影响且无修复版本。
# 为可选回退，缺失时服务优雅降级。详见 SECURITY.md「依赖漏洞现状」。
openai>=1.40.0,<3

# 工具库
tenacity==9.0.0

# 任务调度
APScheduler==3.11.0

# 导出功能（PDF/Excel）
fpdf2==2.7.9
openpyxl==3.1.2

# Markdown 渲染
# 3.3.3 起修复 CVE-2026-76098；该过滤器输出仍会经 bleach 消毒（纵深防御）
mistune==3.3.4

# HTML 净化（生产环境必须，避免 XSS 净化降级）
bleach==6.4.0

# 数据验证（API路由层使用）
pydantic>=2.10.0

# 监控
sentry-sdk==2.20.0
